<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>get 요청</h3>
    <form action="/getForm" method="get"> <!--action = 폼(정보)를 전송할 주소(url), form에서 getForm으로 바뀜-->
    <!--method에는 get(폼에 입력한 정보가 url에 추가, 그대로 노출) 또는 post(폼에서 어떤 정보를 입력했는지 url에서 알 수 없음)-->
        <input type="text" name="id"><!--input태그의 name은 필수사항-->
        <input type="password" name="pw">
        <fieldset>
            <legend>성별</legend>
            <!--하나만 선택 가능, 선택지는 뭘 택했는지 모르기 때문에 value값 줘야 함 -->
            <input type="radio" name="gender" value="male" id="men"> <label for="men">남자</label><!--글씨 클릭해도 선택되게 함-->
            <input type="radio" name="gender" value="female" id="women"> <label for="women">여자</label>
        </fieldset>
        
        <fieldset>
            <legend>취미</legend>
            <!--여러 개 선택 가능-->
            <input type="checkbox" name="interest" value="movie"/> 영화감상
            <input type="checkbox" name="interest" value="music"/> 음악감상
            <input type="checkbox" name="interest" value="excercise"/> 운동
        </fieldset>

        <button type="sumbit">제출</button>
    </form>

    <br><hr><br>

    <h3>post 요청</h3>
    <form action="/postForm" method="post"> 
            <input type="text" name="id"  value='asd' required><!--readonly옵션: 읽기만 가능. 값 수정 불가. 클라이언트 작성불가-->
            <input type="password" name="pw" placeholder="비밀번호" required>

            <select name="birthMonth"> <!--name은 select태그에-->
                <%for ( let i=1; i<=12; i++) {%>
                    <option value="<%=i%>"><%=i%></option> <!--value는 옵션태그에-->
                <% } %>
            </select>

            <textarea name="content" rows="15" cols="50" required></textarea>

            <button type="sumbit">제출</button>
    </form>
    <script>
        console.log(document.forms['getform']) // 'getform'이라는 name을 가진 form태그 선택

    </script>
</body>
</html>